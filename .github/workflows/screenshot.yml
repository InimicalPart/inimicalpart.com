name: Screenshot
on:
  schedule: [{cron: "0 0 * * 0"}]
  workflow_dispatch:
  push: {branches: ["master", "main"]}
jobs:
  web-screenshot:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          filename: site.screenshot.svg
          token: NOT_NEEDED

          # Options
          template: terminal
          base: ""
          plugin_screenshot: yes
          plugin_screenshot_wait: 3500
          plugin_screenshot.viewport: |
            {"width":962, "height":136}
          plugin_screenshot_title: Inimicalpart.com
          plugin_screenshot_url: https://inimicalpart.com
          plugin_pagespeed: yes
          plugin_pagespeed_url: https://inimicalpart.com
          plugin_pagespeed_token: ${{ secrets.PAGESPEED_TOKEN }}

"use client"

import GlassOrbs from "./glass-orbs"

/**
 * Decorative liquid glass orbs for the hero section on the home page.
 * Wraps the shared GlassOrbs component.
 */
export default function LiquidGlassHero() {
  return <GlassOrbs />
}
